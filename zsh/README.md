# Zsh

## Installation

Run `install.sh` to install Zsh plugins and configure it.

## Uninstallation

To uninstall, remove the Zsh configuration files and plugins:

```bash
rm -rf ~/.zshrc ~/.config/zsh
```
